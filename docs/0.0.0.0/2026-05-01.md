# 2026-05-01

## Added Notes
- Player initial position is now `X 0 / Y 150 / Z 0`.
- Added a simple test chunk render for `10 x 10` chunks from chunk start `X 0 / Z 0` toward `X+ / Z+`.
- Test chunks use height `130`; top blocks are grass and lower blocks are rock.
- Test chunk mesh is generated from actual block data and only exposed faces are rendered.
- Fixed terrain vertical camera movement while keeping world `Y+` as up in the 3D projection.
- Fixed terrain horizontal camera basis so world `X+ / Z+` are not mirrored on screen.
- Fixed horizontal mouse look direction by reversing yaw input.
- Fixed sky horizontal projection to match the terrain camera basis.
- Rotated vertical terrain face UVs clockwise by 90 degrees.
- Added a depth attachment and enabled terrain depth testing so farther chunk faces no longer draw over nearer faces.
- Doubled player movement speed from `8` to `16`.
- Added right-side terrain debug stats in `DRAWS`, `FACES`, `VERTICES` order.
- Enabled terrain back-face culling and kept the sprite/text pipeline uncullled.
- Reversed terrain culling front-face direction to match the generated block face winding.
- Added an F4 terrain wireframe toggle using a separate Vulkan line polygon pipeline.
- Added F2 screenshots by copying the rendered swapchain image to a staging buffer and saving BMP files under `screenshots/`.
- Fixed screenshot output path to use the project root `screenshots/` directory instead of the process working directory.
- Changed test chunk terrain to random per-column heights from `130` to `140`, with all generated blocks using `rock`.
- Restored test chunk storage to the full `16 x 512 x 16` size and changed terrain meshing traversal to `16 x 16 x 16` subchunk units.
- Added greedy meshing within each subchunk, merging adjacent visible rock faces by face direction.
- Changed texture sampling to repeat mode so merged greedy faces can tile block textures instead of stretching them.
- Added player skin rendering from `Character.mesh` and `Character.png`.
- Defined player `POS` as the bottom center and first-person eye position as `POS.y + 1.5625`.
- Added F5 view cycling: first person, third-person rear, third-person front.
- Fixed third-person vertical orbit direction so the camera rotates around and looks toward the first-person eye position.

## Work Log
- 개발 기록 작성 방식과 `docs/GUIDE.md` 기준을 정리했다.
- 프로젝트 기본 방향을 정리했다.
- GLFW 3.4 Windows 64-bit 바이너리 배포본을 `external/glfw/` 아래에 배치했다.
- 기본 `.gitignore`를 추가했다.
- Visual Studio CMake 프로젝트로 열 수 있도록 기본 빌드 세팅과 GLFW 창 생성 코드를 추가했다.
- 콘솔 창 제거는 우회 설정이 필요해 기본 콘솔 서브시스템을 유지하기로 했다.
- 하늘 렌더링에 사용할 해와 달 에셋 위치를 확인했다.
- Vulkan swapchain, render pass, command buffer 기반의 최소 렌더러를 추가했다.
- 하늘색 clear 배경 위에 해, 달, 크로스헤어 텍스처를 그리도록 구현했다.
- 마우스 이동으로 카메라 yaw/pitch 회전값을 갱신하도록 구현했다.
- 카메라 위치 이동과 무관하게 하늘 방향은 카메라 회전에만 반응하는 방식으로 구현했다.
- PNG 텍스처 로딩을 위해 `stb_image` 단일 헤더를 `external/stb/`에 추가했다.
- GLSL 스프라이트 셰이더를 추가하고 CMake에서 Vulkan SDK `glslc`로 컴파일하도록 설정했다.
- F11로 전체화면을 토글하도록 추가했다.
- 마우스 상하 회전 방향을 수정했다.
- 좌상단 FPS/프레임 시간 디버그 텍스트를 추가했다.
- 우상단 버전 텍스트를 추가했다.
- 폰트 렌더링을 위해 `stb_truetype` 단일 헤더를 `external/stb/`에 추가했다.
- 디버그 텍스트 테두리를 상하좌우 4방향 오프셋으로 표시하도록 조정했다.
- FPS 문자열 갱신 중 발생한 메모리 오류 가능성을 줄이기 위해 디버그 문자열을 고정 버퍼 방식으로 변경했다.
- 스프라이트 push constant 구조체에 16바이트 정렬을 명시했다.
- 디버그 텍스트의 화면 Y 좌표와 폰트 UV 방향을 수정했다.
- Alt+F4 종료 이벤트가 들어온 프레임에서는 렌더링을 건너뛰도록 수정했다.
- 스프라이트 push constant 전송 구조를 명시적인 `float[12]` 버퍼로 단순화했다.
- 예외 발생 지점을 가리지 않도록 `main`의 catch 블록을 제거했다.
- 우상단 버전 텍스트를 `DOLBUTO 0.0.0.0`으로 변경했다.
- 우상단 버전 아래에 CPU, GPU 하드웨어 이름을 표시하도록 추가했다.
- 우상단 하드웨어 정보 아래에 Vulkan API 버전과 드라이버 버전을 표시하도록 추가했다.
- F3으로 디버그 텍스트 표시를 토글하도록 추가했다.
- 실행 시 마우스를 캡처하고, 휠 클릭으로 해제하며, 좌클릭으로 다시 캡처하도록 추가했다.
- ESC로 게임을 종료하도록 유지했다.
- WASD 수평 이동과 Space 상승, Shift 하강을 추가했다.
- WASD 이동은 카메라 yaw 기준으로만 계산하고 pitch는 반영하지 않는다.
- 좌상단 디버그 텍스트에 플레이어 위치와 시야 방향을 추가했다.
- 시야 방향 표시는 벡터 대신 yaw, pitch, 4방위 이름으로 표시하도록 변경했다.
- 위치 표시는 `POS: X 값 / Y 값 / Z 값` 형식으로 변경했다.
- FPS, 위치, 시야 방향 디버그 갱신 주기를 20Hz로 변경했다.
- CPU, GPU 하드웨어 정보는 접두어 없이 하드웨어명만 표시하도록 변경했다.
- 우상단 디버그 텍스트 줄간격을 넓혔다.
- 우상단 디버그 텍스트에 현재 해상도 표시를 추가했다.
- 디버그 텍스트를 글자별 draw 방식에서 outline/fill 배칭 draw 방식으로 변경했다.
- 디버그 텍스트 배칭 결과와 vertex buffer 업로드를 캐시해 FPS/해상도 변경 시에만 갱신하도록 수정했다.
- 긴 하드웨어 문자열을 감당하도록 디버그 텍스트 배칭 vertex buffer 용량을 늘렸다.
- 배칭 텍스트의 UV V 방향을 수정해 글자 상하 반전을 해결했다.
- Vulkan instance 생성 입력을 최소화하고 GLFW 필수 확장 포인터를 그대로 전달하도록 변경했다.
- 창 생성 전에 GLFW의 Vulkan 지원 여부를 확인하도록 추가했다.
- `assets/data/blocks.json`에 초기 블록 정의를 추가했다.

## Decisions
- 게임은 Vulkan과 C++로 개발한다.
- C++ 표준은 C++20을 기준으로 한다.
- 빌드는 CMake와 Ninja를 사용한다.
- 창 생성과 입력 처리는 GLFW를 사용한다.
- Vulkan SDK는 로컬 설치본을 사용한다.
- GLFW는 Windows 바이너리 배포본을 받아서 사용하는 방식을 기준으로 한다.
- 최종 목표는 마인크래프트류 샌드박스 복셀 게임으로 잡는다.
- 첫 목표는 CMake/Ninja 프로젝트 골격, GLFW 창 생성, Vulkan instance 생성, validation layer 연결, 빈 화면 출력 준비까지로 잡는다.
- 기능을 한 파일에 몰아넣지 않고 역할에 맞게 분리해서 개발한다.
- 구현은 현재 요구사항을 만족하는 가장 단순한 형태로 진행한다.
- 파일, 클래스, 함수는 역할과 책임 기준으로 분리하되, 분리를 위한 분리는 하지 않는다.
- 새 추상화, 공용 시스템, 외부 라이브러리는 필요성이 명확할 때만 추가한다.
- 미래에 필요할 것 같은 기능은 지금 구현하지 않는다.
- 동쪽은 월드 `X+` 방향으로 정의한다.
- 위쪽은 월드 `Y+` 방향으로 정의한다.
- 북쪽은 월드 `Z+` 방향으로 정의한다.
- 블록 좌표 `(x, y, z)`는 해당 블록의 아래면 중앙 위치를 기준으로 한다.
- 초기 해 위치는 정동쪽 수평선 방향으로 둔다.
- 초기 달 위치는 해의 정반대편 방향으로 둔다.
- 해와 달 이미지는 `assets/textures/sky/Sun.png`, `assets/textures/sky/Moon.png`를 사용한다.
- 화면 중앙 크로스헤어는 `assets/textures/ui/Crosshair.png`를 사용한다.
- 하늘은 월드 위치 이동값을 반영하지 않고 카메라 회전만 반영한다.
- 디버그 텍스트는 `assets/fonts/VCR_OSD_MONO.ttf`를 사용한다.
- 디버그 텍스트는 실행 시 기본 표시 상태로 시작한다.
- FPS, 위치, 시야 방향 표시는 초당 20회 갱신한다.
- 디버그 텍스트는 흰 글씨와 검정 테두리로 표시한다.
- 우상단 디버그 정보는 버전, CPU, GPU, Vulkan API 버전, 드라이버 버전, 해상도 순서로 표시한다.
- 해상도 표시는 `RESOLUTION: 가로 x 세로` 형식으로 표시한다.
- 청크 크기는 `X 16 * Y 512 * Z 16`으로 정의한다.
- 청크는 로딩과 언로딩 단위로 사용한다.
- 청크 하나는 Y축 방향으로 `16 * 16 * 16` 크기의 서브청크 32개로 구성한다.
- 서브청크는 메싱 단위로 사용한다.
- 각 블록 셀은 블록 ID와 유체 ID를 함께 가진다.
- 블록 ID와 유체 ID는 각각 65536가지 값을 표현할 수 있도록 `uint16` 범위를 사용한다.
- 블록 ID `0`은 `air`로 예약하며 JSON 블록 정의에 명시한다.
- 초기 블록 ID는 `1 rock`, `2 grass`, `3 dirt`, `4 sand`, `5 sandstone`, `6 mud`, `7 clay`, `8 trunk`, `9 leaves`, `10000 plant`, `65535 bedrock`으로 정의한다.
- 블록 정의 JSON은 `id`, `name`, `renderType`, `textures`를 명시한다.
- 블록 텍스처 값은 확장자 없는 이름으로 작성하고, 실제 파일은 `assets/textures/block/{name}.png`로 해석한다.
- 유체 데이터는 청크 데이터 구조에 포함하지만, 초기 구현에서는 렌더링, 메싱, 시뮬레이션에 사용하지 않는다.
- 유체 데이터의 상위 9비트는 유체 종류, 하위 7비트는 유체량으로 사용한다.
- 유체 없음은 `type = 0`, `amount = 0`으로 표현한다.
- 유체 있음은 `type = 1~511`, `amount = 1~100`으로 표현한다.
- `amount = 100`은 가득 찬 상태를 의미한다.
- `amount = 101~127`은 예약값으로 사용하지 않는다.
- 블록 편집 시 변경 블록이 속한 서브청크를 dirty 처리하고, 경계 블록이면 인접 서브청크도 dirty 처리한다.

## Issues
- 없음.

## Summary
- 문서 기록 방식과 프로젝트의 기본 개발 방향을 정리했다.
