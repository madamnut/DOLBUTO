# 2026-05-03

## Work Log
- 지형 렌더링에 카메라 프러스텀 컬링을 추가했다.
- 프러스텀 판정은 서브청크 AABB 기준으로 처리하고, 프러스텀 밖의 서브청크 메쉬는 draw 호출을 건너뛰도록 했다.
- 우상단 `DRAWS/FACES/VERTICES` 값은 현재 프레임에서 실제 draw된 지형 메쉬 기준으로 갱신되도록 했다.

## Decisions
- 프러스텀 컬링은 일단 CPU에서 단순 AABB 판정으로 처리한다.
- 컬링 단위는 청크 전체가 아니라 현재 GPU 버퍼 단위와 맞는 서브청크로 둔다.
- near/far plane 값은 지형 렌더링과 컬링 판정에서 같은 상수를 사용한다.

## Issues
- 없음.
