# 2026-05-08

## Work Log
- Added water color absorption parameters to `config/render.json`.
- Added SSR Fresnel strength and depth-confidence parameters to water SSR config.
- Updated `fluid.frag` so water becomes deeper-colored based on the linear scene depth behind the water surface.
- Updated SSR hit weighting so edge fade, ray distance, and depth-match confidence reduce unstable reflection hits.
- Replaced custom water Fresnel tuning with reference-style alpha/reflection Fresnel curves.
- Removed `edgeAlpha`, `fresnelPower`, and `ssr.fresnelStrength` from water render config.
- Added `alphaFresnelPower`, `reflectionFresnelPower`, and `reflectionFresnelBase`.
- Removed the experimental water shader effects.
- Simplified `fluid.frag` to texture sampling plus fixed alpha.
- Removed water normal-map loading, SSR config parsing, depth absorption config parsing, and extra fluid descriptor bindings.
- Removed the unused water normal texture asset.
- Reduced water render config to `fluid.water.alpha`.
- Changed water render height to 10 amount steps from `0.08` to `0.8` for top-surface cells.
- Water cells with water directly above now render at full `1.0` block height.
- Moved fluid rendering into the scene pass after blocks, selection, and player rendering so water tests against scene depth.
- Added an F6 climate overlay cycle: off, temperature, precipitation.
- Added a `1024 x 1024` temperature overlay for the full wrapped world, colored blue to red.
- Added a `1024 x 1024` precipitation overlay for the full wrapped world, colored gray to blue.
- Added `climate.precipitation` noise settings to `config/world.json`.
- Added `CLIMATE` status to debug text.
- Climate overlay rendering is fully opaque.
- Added weak tileable temperature noise with a mid-latitude mask.
- Added per-column chunk climate data for temperature and precipitation.
- Saved chunk payloads now include 256 temperature bytes and 256 precipitation bytes after fluid RLE data.
- Changed upper-left debug climate text to `CLIMATE: T[] P[]` sampled at the player column.
- Added binary player state load/save at `saves/world/player.dat`.
- Added runtime text logging under `logs/Latest.txt` and timestamped `logs/DOLBUTO_*.txt` files.
- Added draft prop block definitions for `stone` id `20000` and `branch` id `20001`.
- Changed prop block data to use single `prop.model` and `prop.texture` fields.
- Replaced prop model conversion with startup `.glb` to `.dpm` conversion under `assets/textures/block/model`.
- Added mutually exclusive ground placement ranges for plant, stone prop, branch prop, and tree.
- Added prop rendering through the existing packed terrain quad path.
- Changed packed terrain UV precision from quarter units to 1/256 units for model UVs.
- Changed packed terrain position precision from 1/16 to 1/256 block units for small rotated prop geometry.
- Changed `.glb` prop conversion to merge source triangle pairs back into quad `.dpm` data.
- Fixed GLB root array parsing so top-level `nodes` is not confused with nested scene node lists.
- Fixed prop mesh runtime cache layout so each `.dpm` quad keeps its position and UV data together.
- Added RmlUi 6.1 source under `third_party/RmlUi`.
- Linked RmlUi Core into the game target with samples, Lua, plugins, and the default font engine disabled for the initial dependency step.
- Added the current UI integration direction to `docs/design/ui.md`.
- Added a minimal native lobby overlay with `DOLBUTO`, `START`, and `EXIT`.
- Added an in-game pause overlay opened by `ESC` with `RESUME` and `EXIT`.
- Changed pause `EXIT` to return to the lobby instead of closing the program.
- Disabled player movement, camera rotation, block editing, block selection, and crosshair rendering while lobby or pause overlays are active.
- Stopped the renderer from requesting initial chunk loads while the app is still in the lobby.
- Kept chunk loading active during pause.
- Added a tiled rock texture background to the lobby using repeated sprite UVs.
- Changed the lobby title to use `assets/textures/ui/Title.png`.
- Fixed the initial terrain load after pressing lobby `START`.
- Flipped lobby background and title sprite UVs so `rock.png` and `Title.png` render upright.
- Added FreeType 2.14.2 source under `third_party/freetype-2.14.2`.
- Enabled RmlUi's FreeType font engine.
- Switched the renderer font atlas from `stb_truetype` to FreeType while keeping `assets/fonts/VCR_OSD_MONO.ttf`.
- Separated lobby and game scene lifetime.
- Added explicit game scene load/unload: `START` starts terrain/save workers and resets terrain loading; pause `EXIT` saves player state, stops terrain workers, enqueues all runtime chunks, drains saves, waits for Vulkan idle, clears terrain runtime/render state, and returns to lobby.

## Decisions
- Water render tuning reuses existing push constant slots instead of increasing the push constant layout.
- Water absorption uses scene depth because the current water pass already samples offscreen scene depth for SSR and occlusion.
- Water alpha now approaches full opacity at grazing angles using `fresnel^4`.
- Water reflection now uses a cubic Fresnel curve with a non-zero base reflection weight.
- Advanced water shader work is postponed until the core game systems are further along.
- `amount` remains `0~100`; only the rendered water surface height is quantized.
- Simple water rendering should remain in the scene pass while it depends on terrain/player depth.
- Climate overlays are generated by the game renderer so previews use the same C++ climate functions as runtime generation.
- Temperature noise uses the same 4D torus tiling approach as precipitation.
- Climate is stored as `uint8_t` per chunk column because biome inputs do not need per-block vertical data.
- Player state is stored outside region files as a fixed 41-byte binary layout with no version field.
- Runtime logging writes both a latest log and a timestamped history log for each run.
- Prop blocks stay in the normal block ID layer; their model and texture slot bindings are defined in block data.
- `dpm` stores quad count and quad vertex data, with no magic or version field.
- Ground prop placement reuses the existing plant/tree placement salt so one random byte decides the ground feature.
- Prop meshes are emitted double-sided for now because the current terrain pipeline culls backfaces.
- Prop source models now use `.glb`; generated `.dpm` files are runtime cache data.
- Lobby and menu UI will use RmlUi source integration instead of a paid UI middleware.
- RmlUi backend sample code is reference material; runtime integration should live in engine-side UI modules.
- The first lobby implementation uses the existing native renderer overlay while RmlUi render integration remains pending.
- Lobby background tiling reuses the existing repeat sampler rather than drawing per-tile sprites.
- FreeType is the font rasterizer for native debug and menu text going forward.
- The renderer stays alive in the lobby; game-scene terrain/save workers and terrain runtime state are active only while the game scene is loaded.

## Issues
- Build not run. Builds are performed by the user.
