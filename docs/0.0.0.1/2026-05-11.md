# 2026-05-11

## Work Log
- Removed lower-left chunk queue/save/load debug text and lower-right peak profiler debug text from the in-game debug overlay.
- Added lower-left chunk loading peak-latency diagnostics with `R` reset and accumulated maximum millisecond values for frame, request, ensure, want, and mesh request phases.
- Added resettable chunk request counters for per-frame peak `ensureRuntimeChunk`, saved/empty runtime misses, and `wantRender` dependency request calls.
- Added whole-frame `TOTAL` peak time to the first lower-left chunk diagnostics line.
- Moved saved chunk snapshot reads out of `ensureRuntimeChunk` into a single async chunk-load worker with runtime shells and completed-load installation on the main thread.
- Added an `F1` HUD toggle that hides or shows the normal game-screen hotbar and crosshair without affecting debug text, menus, pause UI, or inventory UI.

## Decisions
- Keep upper-left gameplay/debug text and upper-right hardware/render summary text visible while chunk-loading diagnostics are redesigned.
- Chunk-loading latency diagnostics use accumulated maximums since the last `R` reset instead of current-frame values.
- The HUD visibility toggle is limited to player-facing in-game HUD elements; diagnostic text remains controlled by `F3`.

## Issues
- Broader chunk-loading diagnostics may still need new metrics once the current peak-latency bottleneck is identified.
