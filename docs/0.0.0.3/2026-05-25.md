# 2026-05-25

## 작업 기록
- `stone_shard` 아이템 데이터를 추가했다.
- 표면 소품 블록 `stone`의 드랍을 `rock_chunk`에서 `stone_shard` 1개로 변경했다.
- `stone_shard` 아이템 텍스처 파일을 현재 아이템 텍스처 목록 기준으로 사용하도록 데이터와 설계 문서를 맞췄다.

## 결정 사항
- 지형 암반 계열 `rock` 블록은 기존처럼 `rock_chunk`를 드랍하고, 표면의 작은 `stone` 소품은 뗀석기/파편 계열 재료로 분리하기 위해 `stone_shard`를 드랍한다.

## 이슈
- 빌드는 사용자가 직접 수행한다.
