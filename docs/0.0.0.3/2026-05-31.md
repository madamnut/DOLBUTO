# 2026-05-31

## 추가 작업 기록
- 청크 블록 데이터에 `blockStates` 배열을 추가하고, 저장 payload에는 block entity 뒤 revision 앞에 optional RLE 섹션으로 저장하도록 했다.
- `renderType: "slab"`과 `stateKind: "attach"`를 추가하고, bottom/top/north/south/west/east 반블럭 상태를 지원하도록 했다.
- `dirt_slab` 블록을 추가하고 `dirt` 텍스처를 재사용하는 반칸 메쉬, 레이캐스트, 선택 아웃라인, 배치 충돌을 연결했다.
- `dirt_pile`의 직접 설치를 제거하고, 제작대 `craft`로 `dirt_pile` 4개는 `packed_dirt`, 2개는 `dirt_slab`으로 만들도록 했다.
- `packed_dirt`와 `dirt_slab` 아이템을 추가하고, `block_model` 슬롯/든 아이템/드랍 아이템이 대상 블록의 렌더 타입을 반영하도록 했다.
- 슬랩 조명 전파는 `lightAttenuation`을 낮추지 않고, `blockStates`의 attach 방향에 맞춰 붙은 면 방향으로만 감쇄하도록 했다.

## 작업 기록
- `assets/textures/particle/smoke/smoke_0.png`부터 `smoke_7.png`까지의 연기 파티클 텍스처를 smoke particle texture array로 로드하도록 했다.
- `ParticleRenderPath`에 불 연기 파티클과 활성 fire emitter 목록을 추가했다.
- fire emitter는 블록 설치/제거와 청크 로드/언로드 시점에 갱신하고, 매 프레임 전체 월드를 스캔하지 않도록 했다.
- 연기 파티클은 위로 천천히 상승하고 X/Z 방향으로 약하게 흔들리며, 생성 시점 크기를 유지하도록 했다.
- 연기 파티클은 solid terrain cell과 충돌하면 해당 축 이동을 막고 튕기지 않도록 했다.
- 불 연기 파티클 크기를 `0.8~1.0`블록으로 키우고, 충돌 검사는 중심점 기준으로 단순화했다.
- 연기 파티클은 수명 비율에 따라 `smoke_0`부터 `smoke_7`까지 한 번 재생하고, alpha fade-in/fade-out을 적용하도록 했다.
- particle pipeline에 alpha blending을 켜고, 파티클 vertex의 `alphaBlend` 값을 shader로 전달하도록 했다.
- `charcoal` 아이템을 `assets/data/items.json`에 추가했다.
- 아이템 데이터에 `burnTimeTicks`를 추가하고 초반 식물/나무/석탄 계열 연료 시간을 설정했다.
- `fire` 블록의 남은 연소 시간을 block entity로 저장/로드하고, 연료 드랍 아이템을 소비해 연장되도록 했다.
- 불은 연소 시간이 끝났을 때 같은 셀 영역의 연료 중 가장 짧은 연소 시간 아이템을 1개 소비하고, 연료가 없으면 꺼지도록 했다.
- 블록 데이터에 `breakEffects.particles`를 추가하고, `fire`는 파괴되거나 꺼질 때 깨짐 파티클을 만들지 않도록 했다.
- 플레이어 이동 충돌과 발밑 지지 판정이 `renderType: "slab"` 블록의 attach 상태별 반칸 AABB를 사용하도록 수정했다.
- 플레이어가 ground 상태에서 땅에 닿아 수평 이동 중이면 모든 자세에서 `0.5`블록 step-up을 시도하도록 했다.
- fire가 연료를 소비할 때 최저 연소 시간 아이템이 아니라 무작위 연료를 고르도록 바꾸고, `charcoal`은 다른 연료가 없을 때만 소비되도록 했다.
- fire가 `log` 또는 `stripped_log`를 밀폐된 상태에서 연료로 소비하고, 연소 종료 시점에도 6방향이 막혀 있으면 `charcoal` 4개를 드랍하도록 했다.
- fire block entity에 pit kiln 예약 결과물 `pendingOutputItemId/count`를 저장하도록 했다.
- 밀폐된 fire는 pit kiln 결과 숯이 바로 다시 타지 않도록 `charcoal`을 연료 후보에서 제외하도록 했다.

## 결정 사항
- 불 연기는 월드 데이터가 아니라 렌더만 하는 런타임 파티클로 둔다.
- 연기 파티클은 별도 particle texture array를 사용하되, 기존 particle pipeline과 billboard 렌더 경로를 재사용한다.
- fire 블록 검색은 매 tick 전체 스캔이 아니라 활성 emitter 목록을 갱신하는 방식으로 처리한다.

## 이슈
- 빌드와 실제 화면 확인은 사용자가 직접 진행한다.
