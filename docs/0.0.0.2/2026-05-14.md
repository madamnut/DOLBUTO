# 2026-05-14

## 작업 기록
- `0.0.0.1` 개발을 완료한 것으로 정리하고 `0.0.0.2` 개발 기록 폴더를 시작했다.
- 코드의 시작 로그와 우상단 디버그 버전 표기를 `DOLBUTO 0.0.0.2`로 갱신했다.
- `docs/GUIDE.md`와 설계 문서의 현재 버전 표기를 `0.0.0.2` 기준으로 갱신했다.
- `README.md`의 배포 방식, 조작 키, 디버그 텍스트 설명을 현재 코드 기준으로 갱신했다.
- `docs/design/ui.md`의 RmlUi/FreeType 빌드 통합 설명을 사전 빌드 공유 라이브러리 기준으로 갱신했다.
- `docs/design/world-generation.md`의 precipitation 설정 예시를 현재 `config/world.json` 값과 맞췄다.
- `docs/design/player.md`의 상호작용 거리와 입력 목록을 현재 코드 기준으로 갱신했다.

## 결정 사항
- `0.0.0.1`의 `SUMMARY.md`는 이전 버전 요약으로 유지한다.
- `0.0.0.2`의 개발 기록은 `docs/0.0.0.2/` 아래에 누적한다.
- 빌드는 사용자가 직접 수행한다.
- 현재 README는 별도 포터블 생성 타깃이 아니라 Release 출력 폴더 자체를 포터블 실행 단위로 설명한다.

## 이슈
- 빌드는 실행하지 않았다.
