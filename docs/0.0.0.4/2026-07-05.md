# 2026-07-05

## 작업 기록
- 갓레이/볼류메트릭 라이트 경로를 제거했다. `RendererFrameLoop`의 god ray 호출, god ray pipeline/layout/descriptor/resource, full-res raw/history target, 관련 framebuffer, CMake shader 항목과 shader 파일을 삭제했다.
- 그림자 경로는 유지했다. shadow map, terrain/player shadow pass, terrain shadow sampling, shadow distortion/PCF는 그대로 남겼다.
- 태양/달/불 블룸이 약해지거나 사라져 보이는 문제를 보정하기 위해 `sprite_scene.frag`의 천체 bloom source 출력과 `terrain_lit.frag`/`terrain.frag`의 fire bloom source 출력을 HDR emissive 값으로 강화했다.
- `sprite_scene.frag`, `terrain_lit.frag`, `terrain.frag`를 `glslangValidator -V`로 검증했다.
- 태양/달 screen-space sprite 투영에서 세로 좌표를 반전했던 변경을 되돌렸다.
- 그림자와 갓레이의 광원 방향 산식은 유지하고, `ScreenPresentation::projectSkyDirection`의 화면 Y 투영은 카메라 `up` dot 값을 그대로 사용하도록 복구했다.

## 결정 사항
- 천체 스프라이트가 카메라 pitch와 같은 방향으로 반응해야 하므로 화면 Y 투영은 반전하지 않는다.

## 이슈
- 없음.

## 추가 작업 기록
- 그림자와 갓레이의 sun direction은 유지하고, 태양/달 스프라이트 투영에만 별도의 표시 방향을 사용하도록 분리했다.
- 태양 스프라이트는 조명용 sun direction의 반대 방향을 사용하고, 달 스프라이트는 그 반대편에 배치한다.

## 추가 작업 기록
- 태양/달 스프라이트가 서쪽에서 떠서 동쪽으로 지는 문제가 있어 스프라이트 표시 방향을 다시 조정했다.
- 태양 스프라이트는 `sunLightDirection`을 그대로 투영하고, 달 스프라이트는 그 반대 방향을 사용한다.

## 추가 작업 기록
- 볼류메트릭 라이트 전체 강도를 낮추고, 열린 하늘의 lit 구간보다 shadow 경계 변화가 더 두드러지도록 가중치를 조정했다.
- 태양 고도가 낮으면 갓레이를 약화하고, 최종 additive 색에 상한을 둬 태양 방향을 볼 때 장면이 과하게 하얗게 덮이지 않도록 했다.

## 추가 작업 기록
- 태양/달 스프라이트를 지평선 아래에서 숨기던 조건을 제거했다.

## 추가 작업 기록
- 천체 방향 계산을 `CelestialDirections.h`로 분리했다.
- `sunPositionDirection`은 태양이 있는 방향으로 정의하고 sky, 태양 스프라이트, godray 화면 방향, 지형 태양 조명에 사용한다.
- `sunlightTravelDirection`은 태양 위치 방향의 반대이며 shadow map의 light view forward에만 사용한다.
- sky shader와 태양/달 스프라이트 투영의 화면 Y 해석을 맞춰, 지평선 아래로 내려가는 천체가 화면 위로 올라가는 것처럼 보이지 않게 정리했다.

## 추가 작업 기록
- sky shader와 태양/달 스프라이트 투영이 일반 camera basis가 아니라 지형 렌더링과 같은 `terrainRight/terrainUp/terrainForward` basis를 사용하도록 맞췄다.
- 카메라 pitch 변화에서 지형, sky, 천체 스프라이트가 같은 기준으로 움직이도록 정리했다.

## 추가 작업 기록
- Complementary 계열 쉐이더를 참고해 shadow clip XY에 중심 거리 기반 distortion을 적용했다.
- terrain/player shadow 기록 shader와 terrain/godray shadow sampling shader에 같은 distortion 식을 넣어 shadow map 중심부 해상도가 더 높게 쓰이도록 했다.

## 추가 작업 기록
- 볼류메트릭 라이트를 Complementary 계열 쉐이더의 단순화된 black-shadow 방식으로 다시 작성했다.
- 색유리, 날씨, 구름, 수중 조건은 제외하고, 고정 dither raymarch, distorted shadow 좌표, raw shadow depth visibility, shadow 경계 강조와 shadow memory 기반 광선 누적만 남겼다.

## 추가 작업 기록
- 볼류메트릭 라이트 샘플 분포를 균등 간격에서 가까운 구간이 더 촘촘한 비선형 분포로 변경했다.
- 주변 shadow edge뿐 아니라 ray 진행 방향의 lit/occluded 변화와 shadow memory 이후 다시 lit가 되는 구간을 더 강하게 누적하도록 조정했다.

## 추가 작업 기록
- 볼류메트릭 라이트에 scene-aware intensity를 추가했다.
- 태양 화면 위치 주변과 화면 상단/중앙의 scene depth를 샘플링해 열린 하늘에서는 약하게, 적당한 occluder가 걸린 장면에서는 강하게, 지나치게 막힌 장면에서는 다시 약하게 조절한다.

## 추가 작업 기록
- 볼류메트릭 라이트의 shadow depth 판정에 전용 bias 함수를 추가했다.
- 샘플 거리와 태양 고도에 따라 bias와 shadow edge probe 반경을 조정해 공기 중 shadow 판정이 terrain 표면용 shadow bias에 묶이지 않도록 분리했다.

## 추가 작업 기록
- 볼류메트릭 라이트 최종 광량에 태양 화면 위치 기준 screen-space 방향성 계수를 추가했다.
- 태양 중심부 블룸은 키우지 않고, 태양에서 떨어져 화면 안쪽으로 길게 이어지는 구간만 보강하도록 했다.

## 추가 작업 기록
- 볼류메트릭 라이트를 full-res scene color 직접 누적 방식에서 half-res offscreen target 생성 후 additive composite 방식으로 변경했다.
- god ray 생성 패스는 기존 scene depth/shadow map 샘플링 로직을 유지하되 `waterBlurRenderPass` 기반 half-res target에 기록하고, composite 패스는 선형 샘플링으로 scene color target에 더한다.
- 태양 고도/시야 조건으로 god ray 생성이 생략되는 경우 composite도 생략해 이전 프레임의 half-res target이 scene color에 섞이지 않도록 했다.

## 추가 작업 기록
- half-res 볼류메트릭 라이트 업스케일 과정에서 보이는 줄무늬를 줄이기 위해 god ray composite shader를 단일 샘플에서 3x3 tent 필터로 변경했다.
- 별도 blur pass는 추가하지 않고 composite 단계에서 주변 texel을 함께 섞어 half-res 패턴이 full-res 화면으로 그대로 확대되지 않도록 했다.

## 추가 작업 기록
- half-res 업스케일 방식의 볼류메트릭 라이트 target을 full-res raw target으로 되돌렸다.
- raw god ray 결과와 이전 history를 full-res history target에 섞는 temporal resolve 패스를 추가하고, composite는 resolve된 history target을 scene color에 additive로 더하도록 변경했다.
- history target은 swapchain image별 2개를 ping-pong하고, 갓레이 생성이 생략된 프레임은 해당 image의 history를 무효화해 이전 결과가 다시 섞이지 않도록 했다.

## 추가 작업 기록
- god ray temporal resolve의 history weight를 고정값에서 카메라 이동/회전량 기반 동적 값으로 변경했다.
- 가만히 있을 때는 history 비중을 높이고, 카메라가 움직이거나 회전하면 history 비중을 낮춰 잔상 가능성을 줄인다.
- temporal shader에서 이전 history 색을 현재 raw의 상하좌우 이웃 min/max 범위로 clamp해 밝기 끌림과 ghost trail을 줄이도록 했다.

## 추가 작업 기록
- god ray temporal resolve에 현재 scene depth 기반 edge rejection을 추가했다.
- 현재 픽셀과 상하좌우 depth 차이가 큰 지형/오브젝트 경계에서는 history 비중을 낮추고, depth가 `1.0`에 가까운 하늘 영역은 rejection에서 제외한다.
- resolve descriptor layout에 scene depth용 image binding을 추가하고 descriptor pool의 god ray image descriptor 수를 그에 맞게 늘렸다.
