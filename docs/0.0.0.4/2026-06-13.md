# 2026-06-13

## 작업 기록
- 구운 몰드 블록 9종의 `collision`을 `true`로 변경했다.
- 몰드 충돌은 기존 `renderType: "mold"` 충돌 경로의 중앙 `0.625 x 0.125 x 0.625` AABB를 사용하며, 내부 홈은 충돌에서 무시한다.

## 결정 사항
- 배치된 몰드는 실제 외곽 판이 차지하는 낮은 박스만큼만 충돌하도록 한다.

## 이슈
- 빌드는 사용자가 직접 수행하므로 실행하지 않았다.
