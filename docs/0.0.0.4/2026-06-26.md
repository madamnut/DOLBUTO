# 2026-06-26

## 작업 기록
- 청크 페이드 추가 이후 플레이어 모델과 드랍 아이템 렌더가 검게 보일 수 있는 문제를 수정했다.
- terrain fragment shader를 재사용하는 플레이어, 1인칭 손, 파티클, 드랍 아이템, 손 아이템, 도가니/몰드 용탕 surface 렌더 push constant에 청크 페이드 기본값 `1.0`을 명시했다.

## 결정 사항
- 지형 청크 페이드 대상이 아닌 렌더 경로는 `dynamicLightParams.y`를 항상 완전 표시 값으로 초기화한다.

## 이슈
- 빌드와 런타임 확인은 사용자가 직접 수행한다.
