# 2026-06-11

## 작업 기록
- 몰드 텍스처 20개가 `assets/textures/item/`에 추가된 것을 확인했다.
- `assets/data/items.json`에 몰드 아이템 19개를 추가했다.
- 추가한 몰드 아이템은 `unfired_mold_base`, 굽기 전 몰드 9종, 구운 몰드 9종이다.
- 모든 몰드 아이템은 `extruded_sprite` 렌더 타입과 `stackSize = 1`을 사용한다.
- `mold_base.png`는 현재 아이템 데이터에 연결하지 않고 더미 텍스처로만 둔다.
- `docs/design/item-data.md`에 몰드 텍스처, 아이템 ID, 스택 규칙을 반영했다.

## 결정 사항
- 몰드는 이후 용탕 종류, 주입량, 냉각 진행도 같은 인스턴스 상태를 가질 수 있으므로 스택을 1로 제한한다.
- 기본 몰드 베이스는 굽기 전 상태인 `unfired_mold_base`만 실제 아이템으로 등록한다.

## 이슈
- 몰드 제작 레시피, 소성 레시피, 용탕 주입 결과물은 아직 구현하지 않았다.
- 빌드는 사용자가 직접 수행하는 규칙에 따라 실행하지 않았다.
